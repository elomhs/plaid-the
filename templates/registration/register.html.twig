{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
    <h1>Register</h1>

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.username) }}
        {{ form_row(registrationForm.plainPassword) }}
        <br>
        <button class="btn" style="background-color: #9fcdff" >Client</button>

        <button class="btn" style="background-color: #9fcdff" >Loueur</button>
        <br>
        <br>
        <button class="btn" style="background-color: #9fcdff">S'enregistrer</button>
    {{ form_end(registrationForm) }}
{% endblock %}
