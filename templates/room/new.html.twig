{% extends 'base.html.twig' %}

{% block title %}New Room{% endblock %}

{% block body %}
    <h1>Create new Room</h1>

    {{ include('room/_form.html.twig') }}

{#    {{ dump(room) }}#}
{#    {{ dump(room.owner) }}#}
    <a href="{{ path('room_index', {'id':room.id}) }}">back to list</a>
{% endblock %}
