{% extends 'base.html.twig' %}

{% block title %}Region{% endblock %}

{% block body %}
    <div id="wrapper" class="container">

        <section id="main" class="row justify-content-center">
            <!-- Header -->
					<br>
					<header id="header">
						<h1 style="font-size: 50px"> Chambres disponibles dans la région </h1>
					</header>

                <!-- Thumbnails -->
							<section class="thumbnails " >

								<div style="width: 400px">
									{% for room in region.rooms %}
									<a href="{{ path('room_show', {id:room.id}) }}">
										<br>
										<h1>{{ room.summary }}</h1>
										<img src="{{ vich_uploader_asset(room, 'imageFile') }}"/>
{#										<img src="{{ asset('images/thumbs/'~room.image) }}" alt="" />#}
										<h3>{{ room.description }}</h3>
									</a>
									{% endfor %}
								</div>

							</section>
            </tr>

        </section>
    </div>

    <a href="{{ path('region_index') }}">back to list</a>
	{% if is_granted('ROLE_SUPER_ADMIN') %}
    <a href="{{ path('region_edit', {'id': region.id}) }}">edit</a>

    {{ include('region/_delete_form.html.twig') }}
	{% endif %}
{% endblock %}
